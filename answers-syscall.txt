According to the backtrace output, usertrap() functon called syscall.

The value of p->trapframe->a7 is 0x7, representing exec system call number.

lw a3,o(zero) , a3 corresponds to the variable num.

When the kernel paniced, the name of the running binary is "initcode", corresponding pid is 1.